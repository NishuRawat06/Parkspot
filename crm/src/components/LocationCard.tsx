"use client";

/**
 * Backwards-compatible entry point.
 * Real implementation lives in `src/components/location/LocationCard.tsx`.
 */
export { default, type Location, type LocationCardProps } from "@/components/location/LocationCard";
